[Unit]
Description=Register Gainmaster User Service service id %i to Vulcand

Requires=etcd.service
After=etcd.service

BindsTo=gainmaster-user-service@%i.service
After=gainmaster-user-service@%i.service

[Service]
EnvironmentFile=/etc/metadata
EnvironmentFile=/tmp/gainmaster_user_service_env
RemainAfterExit=yes

ExecStart=/usr/bin/etcdctl set /vulcand/backends/gainmaster-user-service/servers/%i '{"URL": "${SERVICE_ADDRESS}"}'

ExecStartPost=/usr/bin/etcdctl set /vulcand/backends/gainmaster-user-service/backend '{"Type": "http"}'
ExecStartPost=/usr/bin/etcdctl set /vulcand/frontends/gainmaster-user-service/frontend '{"Type": "http", "BackendId": "gainmaster-user-service", "Route": "Host(`api.hesjevik.im`) && PathRegexp(`/users.*`)"}'

ExecStop=/bin/etcdctl rm /vulcand/backends/gainmaster-user-service/servers/%i 

ExecStopPost=/usr/bin/bash -c '\
  SERVICE_STATUS=$(curl -f -s -I api.hesjevik.im/users | grep HTTP/1.1); \
  if [ -z "$SERVICE_STATUS" ]; then \
      /usr/bin/etcdctl rm /vulcand/backends/gainmaster-user-service --recursive; \
      /usr/bin/etcdctl rm /vulcand/frontends/gainmaster-user-service --recursive; \
  fi'

[X-Fleet]
MachineOf=gainmaster-user-service@%i.service